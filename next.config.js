/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['static.finnhub.io'],
  },
}

module.exports = {
  reactStrictMode: true,
  images: {
    domains: ['static.finnhub.io'],
  },
}
